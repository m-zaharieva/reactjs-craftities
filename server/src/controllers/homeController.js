import { Router } from "express";

import Listing from '../models/Listing.js';



const router = Router();


router.get('/', (req, res) => {
    //TODO get home page required data and return it
})



export default router;